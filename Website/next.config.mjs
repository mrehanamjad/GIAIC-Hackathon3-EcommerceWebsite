/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['plus.unsplash.com','images.unsplash.com','next-ecommerce-template-4.vercel.app'],
    }
};

export default nextConfig;
